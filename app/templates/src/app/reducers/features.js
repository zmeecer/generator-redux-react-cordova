const initialState = {
  features: [
    'React',
    'Redux',
    'ES6',
    'Webpack',
    'CSS Modules',
    'Cordova',
    'Phonegap',
  ],
};

export function features(state = initialState) {
  return state;
}
