import React from 'react';
import { Subheader } from '../../components';
import styles from './styles.scss';

const More = () => (
  <div>
    <Subheader
      title="More"
    />
  </div>
);

More.propTypes = {
};

export default More;
